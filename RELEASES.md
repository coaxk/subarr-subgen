# subarr-subgen releases

Patch-rev provenance for the `subarr-subgen` quilt stack. Each row
captures one shipped revision against a known-good upstream subgen
commit. Yank a row when a real-world install hits a regression we
can't hotfix in-place — the `status` column drops to `yanked` and the
`yank_reason` explains why.

This file is the canonical "is patch v4.x safe to apply?" reference.
subarr's update checker reads it via the GitHub API on every health
probe; a `yanked` rev triggers a warning chip in Settings → Updates.

## Format

Add a new row at the top of the table for every shipped patch rev.
Format: pipe-separated markdown. Dates are ISO; status values are
`healthy`, `superseded`, or `yanked`.

| Rev    | Status     | Date       | Upstream sha | Notes |
|--------|------------|------------|--------------|-------|
| v4.19  | healthy    | 2026-07-21 | 82c64af      | **Upstream sync -> subgen 2026.07.1** (#26, closes #21). Rebased the patch stack onto upstream `82c64af`, picking up three upstream releases (2026.06.5, 2026.06.6, 2026.07.1). Three patches needed porting: **0003** (upstream rewrote `transcribe_existing` to add the `.subgen_skip` directory marker and a `SKIP_STARTUP_SCAN` early-out; the marker walk-pruning is carried over verbatim), **0009** (version-line context only), and **0024** (upstream restructured `should_skip_file` section 4 behind a `named_output_configured` guard, and split `subtitle_exists_in_language` into internal/external locals where `internal` is now gated on `only_match_subgen_subtitles`; our `ignore_forced_override` threads through the internal call and leaves upstream's new gate intact). **New patch 0033 - deliberate divergence from upstream:** upstream implemented `SKIP_STARTUP_SCAN` as an early-out INSIDE `transcribe_existing()`, but `POST /batch` calls that same function, so `SKIP_STARTUP_SCAN=1` would silently turn every subarr-requested transcription into a no-op returning zero counts with no error. We gate the boot scan at the `lifespan` call site instead, preserving upstream's intent without breaking explicit requests. Reported upstream as McCloudS/subgen#345. Also inherits upstream's `.subgen_skip` marker + `SKIP_STARTUP_SCAN` env var, the `SUBTITLE_LANGUAGE_NAME` unknown-audio fix, and the `SKIP_ONLY_SUBGEN_SUBTITLES` embedded-stream fix. Capabilities UNCHANGED from v4.18, so subarr behaviour is identical. Verified: 33 patches apply cleanly, validator 37/37, and new `tests/smoke_patched_behaviour.py` proves at runtime that marker pruning, reverse sort, structured counts and both halves of the 0033 contract all hold. Ships as image `2026.07.1-r1`. Supersedes r4. |
| v4.18  | superseded | 2026-06-30 | 0529a2a      | **Fix `detect_language_robust` per-chunk probability `p=0.00`** (coaxk/subarr#396, patch 0031). stable_whisper's `load_faster_whisper` transcribe returns a `WhisperResult` built from `dict(language=, segments=)` only — it drops faster-whisper's `info.language_probability` — so the old `getattr(res, 'language_probability', 0.0)` was **always 0.0** and every chunk reported `p=0.00` in the robust-detect evidence (the aggregate `min_probability` too). Now calls faster-whisper's `WhisperModel.detect_language(audio=…)` directly (the `load_faster_whisper` object **is** a `WhisperModel`) for the real per-chunk probability; it's language-only (no full transcription) so it's also **faster** than the previous per-chunk transcribe. Live-verified: a known-English clip now reports per-chunk `p≈0.90/0.93` instead of `0.00`. subarr's display side needed no change. Additive + backward-compatible. Ships as image `2026.06.4-r4`. Supersedes r3. |
| v4.17  | superseded | 2026-06-24 | 0529a2a      | **Async `/config` model switch** (subarr-subgen #6 item 1, patch 0029). `POST /config?wait=false` kicks the model/compute switch on a **background thread** and returns **202** immediately — a switch to an uncached model (download ~2.5 min) no longer blows subarr's 120s client read timeout. The switch progress is surfaced as `config_switch: {state: idle\|switching\|done\|failed, model, compute_type, reason, detail, rolled_back}` in the `GET /queue` body for the client to poll; advertises `capabilities.async_config`. **Backward-compatible:** default `wait=true` keeps the synchronous contract (pre-`async_config` subarr is unaffected), and the teardown+reload (incl. the v4.16 `RLock`/rollback contract) is shared between both paths via `_perform_config_switch`. A switch already in progress returns 409. Ships as image `2026.06.4-r3`. Supersedes r2. |
| v4.16  | superseded | 2026-06-24 | 0529a2a      | **Security hardening + per-track detect + /config polish** (subarr-subgen #13/#17/#6). **#13 path containment (HIGH security):** `/asr ?path=`, `/batch ?directory=`, `/detect_language_robust ?path=` now enforce a realpath allowlist (`SUBGEN_PATH_ALLOWLIST`, colon-separated, default `/media`; `os.path.realpath` is symlink-safe) — closes path-traversal → container-secret exfil (`POST /asr?path=/proc/1/environ`) and whole-FS walk (`/batch?directory=/`) on the unauthenticated-by-design LAN. Also `detect_language_robust` now counts toward `active_direct_tasks` so `/config`'s busy-check can't unload the model mid-detection. **#17 per-track detect:** `detect_language_robust` accepts `?track=N` (0-based audio stream → `-map 0:a:N`) so subarr can language-detect a specific non-default audio track (the Review track-mismatch confirmation loop); advertises `capabilities.detect_language_track`. **#6 /config polish:** `/queue` caps emit live `model`/`compute_type` (`Config.Env` goes stale after a runtime swap); the `/config` model swap now holds `model_load_lock` (`Lock` → `threading.RLock()` for `start_model()`'s nested acquire) across the whole teardown+reload, closing the non-atomic busy-check-vs-swap race. Additive + backward-compatible (empty `SUBGEN_PATH_ALLOWLIST` opts out of containment). Ships as image `2026.06.4-r2`. Supersedes r1. |
| v4.15  | superseded | 2026-06-23 | 0529a2a      | **Upstream sync → subgen 2026.06.4** (#15). Rebased the patch stack onto upstream `0529a2a`. **Dropped `0019-ignore-forced-subtitles` (#79):** upstream independently implemented `IGNORE_FORCED_SUBTITLES` (commits `74dd4aa`+`a730f4f`, default **True**) on the same function — we now use upstream's native forced-exclusion instead of our own. **Re-ported `0024` (per-request `ignore_forced`, #317 Slice B)** onto upstream's forced code: `has_internal_subtitle_in_language` gains the `ignore_forced_override` param and the forced check honours it (`_eff_ignore_forced`). Capabilities UNCHANGED from r10 (`request_ignore_forced` + `ignore_forced_subtitles` still advertised on `/queue`), so subarr behaviour is identical — only the **default flips to upstream's `True`** (forced-only files auto-transcribe unless `IGNORE_FORCED_SUBTITLES=False`). Also inherits upstream's OpenAI-compatible `/v1/audio/*` endpoints; our Dockerfile was already on CUDA 12.8 (Blackwell). Ships as image `2026.06.4-r1`. Supersedes the `2026.05.3-r10` build below. |
| v4.15  | superseded | 2026-06-22 | b4fbc8d      | Per-request `ignore_forced` (#317 Slice B): POST /batch accepts `ignore_forced=true`, overriding the global `IGNORE_FORCED_SUBTITLES` for that one batch — threaded `transcribe_existing → gen_subtitles_queue → should_skip_file → subtitle_exists_in_language → has_internal_subtitle_in_language` (only the step-4 target-language existence check; the `skip_if_internal_sub_language` check keeps the global). Lets subarr's "transcribe a full sub anyway" action fill a forced-only gap per-file without flipping the global knob. Advertises `capabilities.request_ignore_forced` so subarr only offers the action against a subgen that honours it (older subgen silently drops the unknown param and re-skips). Default off = identical to before. Additive + backward-compatible. Ships as image `2026.05.3-r10`. |
| v4.14  | superseded | 2026-06-13 | b4fbc8d | r9 guided-setup, Track 1 (repo commit 5db497b, image `dev-2026.05.3-5db497b`). **Baked tuned defaults**: Phase-1 `SUBGEN_KWARGS` (+ `SUBGEN_KWARGS_LANG_JA`) ship as image-default env. Two knobs are deliberate contested choices, not oversights: `condition_on_previous_text=false` (trades cross-segment context for hallucination-loop resistance) and the VAD timing block (`threshold` 0.5 / `min_speech_duration_ms` 250 / `min_silence_duration_ms` 500 / `speech_pad_ms` 600). Both are plain env defaults — set your own `SUBGEN_KWARGS` to override entirely. **Entrypoint device-guard**: when a GPU is visible and `TRANSCRIBE_DEVICE` is unset, fills `TRANSCRIBE_DEVICE=cuda` (+ `COMPUTE_TYPE=float16`); fill-only-unset, an explicit env value always wins. **Release tag**: `SUBARR_SUBGEN_RELEASE_TAG` baked at build and emitted as `subarr_subgen_release_tag` in GET /queue caps — the image side of coaxk/subarr#224. **POST /config**: runtime model/compute switch with validation (`ok:false` + reason on unknown model) and a rollback contract — a failed load restores the previous model, the server never ends up modelless. Advertises `capabilities.runtime_config`. Additive + backward-compatible. |
| v4.13  | healthy    | 2026-06-06 | b4fbc8d      | `IGNORE_FORCED_SUBTITLES` (#79): a "forced" target-language embedded subtitle track only covers the foreign-language dialogue in an otherwise native-language film — partial coverage, not a full transcript. Vanilla subgen treats it as "subtitle already exists" and skips the file, so a correctly-flagged forced-only gap can never be filled. When `IGNORE_FORCED_SUBTITLES` (legacy `SUBGEN_IGNORE_FORCED_SUBS`) is on, forced subtitle streams are ignored in `has_internal_subtitle_in_language` so a forced-only file gets transcribed instead of skipped. Version-tolerant `_subtitle_stream_is_forced` reads the pyav disposition defensively (False on any uncertainty). Advertises the RUNTIME flag value as `capabilities.ignore_forced_subtitles` so subarr only offers a forced-only row as actionable when the connected subgen will actually fill it. Default OFF = vanilla behaviour preserved. Additive + backward-compatible. **r5 also bundles `0020-subgen-kwargs-json-parse`** (no rev/capability change): `SUBGEN_KWARGS` is JSON in compose, but the parser used `ast.literal_eval` only → JSON booleans raised `ValueError` → tuned kwargs silently fell back to `{}` and never applied (Tier-B 2026-06-04). Now JSON-first with a Python-literal fallback and a loud `logging.warning`. |
| v4.12  | superseded | 2026-06-06 | b4fbc8d      | `/asr` detected-language header (#131): the `/asr` endpoint returns the Whisper-detected source language in an `X-Detected-Language` response header (with `Access-Control-Expose-Headers` so a browser client can read it), carried out of the worker on `TaskResult.language`. subarr's tuning-lab arena captures it to label sweeps + (later) group/aggregate results per source language. Advertises `capabilities.asr_detected_language`. Additive + backward-compatible. |
| v4.11  | superseded | 2026-06-06 | b4fbc8d      | `/asr ?base=vanilla` (#131): POST /asr accepts `base=vanilla`, which runs the recipe against library defaults + the per-request kwargs ONLY — the global `SUBGEN_KWARGS` and the per-language layers are skipped. Makes a tuning-lab sweep a property of the recipe, not of the operator's accumulated env, so results are comparable across users (federated #124). Base folded into the dedup hash (a vanilla run must not collide with a global run of the same file) + the file-path task_id shortcut gated to the plain global path. Advertises `capabilities.asr_vanilla_base`. Additive + backward-compatible (default `global`). |
| v4.10  | superseded | 2026-06-05 | b4fbc8d      | `/asr` arena channel (#131): POST /asr accepts `path=` (read audio from a subgen-visible path — no upload, leverages the shared media mount) + `kwargs=<json>` (per-request override), still returning the sub over HTTP. The no-shared-scratch arena path: subarr passes a path + kwargs, subgen reads + transcribes/translates + returns the srt, nothing touches a shared writable disk. kwargs folded into the dedup hash (fixes a latent collision). Advertises `capabilities.asr_arena`. Additive + backward-compatible. |
| v4.9   | superseded | 2026-06-04 | b4fbc8d      | Per-request task (#131): POST /batch accepts a `task=transcribe\|translate` query param that overrides the global `TRANSCRIBE_OR_TRANSLATE` for that batch only. Pairs with v4.8 kwargs so subarr's tuning-lab arena can drive a source-transcribe AND candidate-translate through one path-based channel. Advertises `capabilities.per_request_task`. Additive + backward-compatible. |
| v4.8   | superseded | 2026-06-04 | b4fbc8d      | Per-request kwargs channel (#88): POST /batch accepts a `kwargs=<json>` query param that overrides global + per-language `SUBGEN_KWARGS` for that batch only (per-request wins). Lets subarr's tuning-lab/tournament arena trial configs against the live model without rewriting env + restarting. Advertises `capabilities.per_request_kwargs`. Additive + backward-compatible. |
| v4.7   | superseded | 2026-06-01 | b4fbc8d      | Safe-decode preset: hardens the decode path against malformed/edge-case audio so a single bad file can't wedge the worker. |
| v4.6   | superseded | 2026-06-01 | b4fbc8d      | Curated per-language Whisper prompts (`SUBARR_SUBGEN_DEFAULT_PROMPTS`) — seeds the decoder with language-appropriate priming text. |
| v4.5   | superseded | 2026-06-01 | b4fbc8d      | `POST /detect_language_robust` — N-chunk Whisper language detection with majority vote + min-probability aggregate. Layer 3 of subarr's audio-ground-truth funnel. Advertises `capabilities.robust_language_detection`. |
| v4.4   | superseded | 2026-06-01 | b4fbc8d      | `POST /queue/cancel?path=…` — cancel a queued task by canonical path; structured reason when not cancellable. Advertises `capabilities.queue_cancel`. |
| v4.3   | superseded | 2026-05-31 | b4fbc8d      | Adds `audio_language_override` query param on POST /batch + capability advertisement (`subarr_subgen_patch_rev`, `capabilities.audio_language_override`) on GET /queue. Lets subarr verified-audio rows bypass `SKIP_IF_AUDIO_LANGUAGES=eng` without disabling the skip-list globally. |
| v4.2   | superseded | 2026-05-28 | b4fbc8d      | Added GET /queue endpoint (dict-keyed `DeduplicatedQueue`). Foundation for subarr's queue monitor. |
| v4.1   | superseded | 2026-05-27 | 69ecaca      | POST /batch structured response: `{walked, queued, skipped, already_in_queue, no_audio, pending_language_detect}` instead of bare text. Lets subarr scan_runner classify path outcomes. |
| v4.0   | superseded | 2026-05-26 | 69ecaca      | Per-language `SUBGEN_KWARGS_LANG_<CODE>` block resolution. Auto-picks the right block based on detected/forced audio language. |

## Procedure: yank a rev

1. Confirm the failure mode is REAL — got a stack trace + repro from a
   user OR our own staging install with the patched build.
2. Update this file: change the row's status to `yanked`, append
   `yank_reason` after the existing Notes content. Push to main.
3. If the regression is fixable forward, ship the fix as a new rev
   (e.g. v4.3 yanked → v4.4 healthy). subarr's UI will surface "patch
   v4.3 yanked — upgrade to v4.4" without manual intervention from the
   user.
4. If not fixable forward in a sane time, edit the apply-patches.sh
   `default rev` constant down to the last healthy rev so fresh
   `subarr-subgen` clones don't apply the yanked patch.

## Procedure: superseding a rev

Just append the new row at the top with status `healthy` and flip the
previous rev's status to `superseded`. Superseded ≠ broken; older
deployments that haven't upgraded yet keep working. Yank a superseded
rev only if you discover a bug AFTER it's no longer current — same
yank procedure applies.

## subarr's update checker

`/api/updates/state` in subarr reads the latest `healthy` rev from
this file and compares against the running subgen's `subarr_subgen_patch_rev`
field (advertised via GET /queue). When the user's rev is older, the
update banner offers the upgrade with a one-click compose-edit
walkthrough. When the user's rev is `yanked`, the banner switches to a
red warning with the `yank_reason` text.
