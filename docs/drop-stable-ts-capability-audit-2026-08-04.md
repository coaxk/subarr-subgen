# refactor/drop-stable-ts: capability audit

Generated by `scripts/capability_audit.py`. Every hunk in `patches/` is applied against two pristine trees and judged by whether `git apply` accepts it.

- **Branch probed**: `refactor/drop-stable-ts` @ `7997624`
- **Pinned base**: `f38dcaa` (the commit `upstream/` sits on)
- **Hunks probed**: 129 across 34 patches
- **Stack-wide totals**: INTACT 31, BROKEN_BY_BRANCH 11, INCONCLUSIVE 86, SKIPPED_OTHER_FILE 1

## Method: apply the hunk, do not match the name

An earlier design compared upstream symbol NAMES between base and branch. It was scrapped because it could not fire: of the upstream symbols our patches attach to, this branch removes exactly **zero**, so a name-matcher emits all-green by construction. Meanwhile `args.update(kwargs)` -- the real attachment point for the per-request kwargs family -- went from **3 occurrences to 0** while all three enclosing function names survived. The name is not the seam. `seams_for_patch` remains in the module as a triage index and is **not** the verdict.

Each hunk is reconstructed as a standalone patch and offered to both trees:

| Applies to base | Applies to branch | Outcome |
| --- | --- | --- |
| yes | yes | `INTACT` -- the code this hunk lands on still exists |
| yes | no | `BROKEN_BY_BRANCH` -- decisive: the seam is gone |
| no | either | `INCONCLUSIVE` -- see the caveat below |

`git apply` tolerates a line-number offset but requires the context to match exactly, so `INTACT` means the surrounding code is still there verbatim, not merely that a function of the same name is.

## Capabilities

| Capability | Providing patches | Outcomes |
| --- | --- | --- |
| `asr_arena` | `0016-asr-path-and-kwargs.patch` | INTACT 6, BROKEN_BY_BRANCH 1, INCONCLUSIVE 3 |
| `asr_detected_language` | `0018-asr-detected-language.patch` | INTACT 2, BROKEN_BY_BRANCH 2, INCONCLUSIVE 1 |
| `asr_vanilla_base` | `0017-asr-vanilla-base.patch` | INCONCLUSIVE 9 |
| `async_config` | `0029-async-config-switch.patch` | INCONCLUSIVE 6 |
| `audio_language_override` | `0009-audio-language-override.patch` | INTACT 1, BROKEN_BY_BRANCH 1, INCONCLUSIVE 6 |
| `concurrent_transcriptions` | `0023-expose-concurrent-transcriptions.patch` | INCONCLUSIVE 1 |
| `curated_language_prompts` | `0012-curated-language-prompts.patch` | INCONCLUSIVE 3 |
| `detect_language_track` | `0027-config-live-model-and-swap-lock.patch` | INTACT 1, INCONCLUSIVE 4 |
| `ignore_forced_subtitles` | `0022-runtime-config-endpoint.patch` | INTACT 1, INCONCLUSIVE 3 |
| `per_request_kwargs` | `0014-per-request-kwargs.patch` | INTACT 2, INCONCLUSIVE 8 |
| `per_request_task` | `0015-per-request-task.patch` | INCONCLUSIVE 7 |
| `queue_cancel` | `0010-queue-cancel.patch` | INCONCLUSIVE 4 |
| `request_ignore_forced` | `0024-per-request-ignore-forced.patch` | INTACT 4, INCONCLUSIVE 10 |
| `robust_language_detection` | `0011-robust-language-detection.patch` | INTACT 1, INCONCLUSIVE 2 |
| `runtime_config` | `0022-runtime-config-endpoint.patch` | INTACT 1, INCONCLUSIVE 3 |
| `safe_decode_preset` | `0013-safe-decode-preset.patch` | INCONCLUSIVE 3 |

### Broken hunks, by capability

**`asr_arena`**

- `@@ -1315,17 +1354,23 @@ def asr_task_worker(task_data: dict) -> None:`

**`asr_detected_language`**

- `@@ -1445,7 +1456,9 @@ def asr_task_worker(task_data: dict) -> None:`
- `@@ -1475,7 +1488,9 @@ def asr_task_worker(task_data: dict) -> None:`

**`audio_language_override`**

- `@@ -1,4 +1,8 @@`

### Every broken hunk, stack-wide

The grouping above can only name breaks inside patches that add a capability literal. This is **all** of them, including the patches no capability row can reach -- see caveat 2.

| Patch | Hunk |
| --- | --- |
| `0001-per-language-kwargs.patch` | `@@ -1077,6 +1090,16 @@ def asr_task_worker(task_data: dict) -> None:` |
| `0001-per-language-kwargs.patch` | `@@ -1279,6 +1302,16 @@ def detect_language_from_upload(task_data: dict) -> None:` |
| `0001-per-language-kwargs.patch` | `@@ -1595,6 +1628,16 @@ def gen_subtitles(file_path: str, transcription_type: str, force_language: Langu` |
| `0006-fastapi-jsonresponse-import.patch` | `@@ -70,7 +70,7 @@ import requests` |
| `0007-deduplicated-queue-type-tracking-and-queue-endpoint.patch` | `@@ -561,6 +567,21 @@ def webui():` |
| `0008-log-language-detection-probability.patch` | `@@ -1343,7 +1343,7 @@ def detect_language_from_upload(task_data: dict) -> None:` |
| `0008-log-language-detection-probability.patch` | `@@ -1428,7 +1428,7 @@ def detect_language_task(path, original_task_data=None):` |
| `0009-audio-language-override.patch` | `@@ -1,4 +1,8 @@` |
| `0016-asr-path-and-kwargs.patch` | `@@ -1315,17 +1354,23 @@ def asr_task_worker(task_data: dict) -> None:` |
| `0018-asr-detected-language.patch` | `@@ -1445,7 +1456,9 @@ def asr_task_worker(task_data: dict) -> None:` |
| `0018-asr-detected-language.patch` | `@@ -1475,7 +1488,9 @@ def asr_task_worker(task_data: dict) -> None:` |

### CUSTOM_REGROUP -- **GONE**

This is the one genuine seam loss, and the tool above **cannot find it**. CUSTOM_REGROUP advertises no capability flag, so nothing in `ADVERTISED_CAPABILITIES` derives it and no row above covers it -- it is hard-coded into this report precisely so it can never be dropped by omission. Patches 0001 and 0017 set it via `args['regroup'] = ...`, a dict assignment the capability regex never matches by design.

On the branch, `regroup` survives in exactly two places and neither is a live use: a comment (`# Replaces stable-ts WhisperResult, Segment, and regroup machinery.`) and the frozenset `_STABLE_TS_KWARGS = frozenset({'regroup', 'input_sr', 'progress_callback'})`. That frozenset is a **strip-list**: both transcribe call sites do `fw_kwargs = {k: v for k, v in kwargs.items() if k not in _STABLE_TS_KWARGS}` before `**fw_kwargs` reaches faster-whisper. So `regroup` is not merely unused -- it is **explicitly filtered out**. Zero live uses. This capability cannot be re-ported at any price and is the veto candidate.

## How to read these numbers

**1. INCONCLUSIVE was NOT counted as breakage, so every count here is a lower bound on the damage.** A hunk that fails on the pinned base fails because it depends on text an earlier patch in the stack creates -- the probe applies each hunk standalone, so it simply cannot judge those. Scoring them broken would invent damage; scoring them intact would hide it. They are reported as their own outcome and excluded from the verdict, which means the true breakage is at least the `BROKEN_BY_BRANCH` count and possibly more. Resolving them requires applying the stack cumulatively, which is Phase 2 work.

**2. `build_capability_map` under-counts providers, one-directionally.** It maps a capability to the patches that add its flag literal, so an implementation patch that never touches the literal is invisible to it. `runtime_config` maps to `0022` alone even though `0027` and `0029` also carry its seam. The bias only ever **shrinks** the evidence set, never inflates it -- so a capability showing breakage here is real, while a capability showing none may simply have had its breaking patch left out of the mapping.

**3. `SKIPPED_OTHER_FILE` is out of scope, not passing.** Only `subgen.py` is materialised, because that is the file this branch refactors. Hunks targeting anything else (`entrypoint.sh`, from `0021`) are counted and reported rather than dropped, so the denominator stays honest.

## Whole-stack census, by patch

Every hunk in `patches/`, including patches that map to no advertised capability. This is the denominator the per-capability table above cannot show.

| Patch | Hunks | Outcomes |
| --- | --- | --- |
| `0001-per-language-kwargs.patch` | 4 | INTACT 1, BROKEN_BY_BRANCH 3 |
| `0002-eager-model-load.patch` | 1 | INTACT 1 |
| `0003-reverse-sort-transcribe-existing.patch` | 2 | INTACT 1, INCONCLUSIVE 1 |
| `0004-batch-structured-response.patch` | 1 | INTACT 1 |
| `0005-gsq-return-str.patch` | 3 | INTACT 3 |
| `0006-fastapi-jsonresponse-import.patch` | 1 | BROKEN_BY_BRANCH 1 |
| `0007-deduplicated-queue-type-tracking-and-queue-endpoint.patch` | 5 | INTACT 4, BROKEN_BY_BRANCH 1 |
| `0008-log-language-detection-probability.patch` | 2 | BROKEN_BY_BRANCH 2 |
| `0009-audio-language-override.patch` | 8 | INTACT 1, BROKEN_BY_BRANCH 1, INCONCLUSIVE 6 |
| `0010-queue-cancel.patch` | 4 | INCONCLUSIVE 4 |
| `0011-robust-language-detection.patch` | 3 | INTACT 1, INCONCLUSIVE 2 |
| `0012-curated-language-prompts.patch` | 3 | INCONCLUSIVE 3 |
| `0013-safe-decode-preset.patch` | 3 | INCONCLUSIVE 3 |
| `0014-per-request-kwargs.patch` | 10 | INTACT 2, INCONCLUSIVE 8 |
| `0015-per-request-task.patch` | 7 | INCONCLUSIVE 7 |
| `0016-asr-path-and-kwargs.patch` | 10 | INTACT 6, BROKEN_BY_BRANCH 1, INCONCLUSIVE 3 |
| `0017-asr-vanilla-base.patch` | 9 | INCONCLUSIVE 9 |
| `0018-asr-detected-language.patch` | 5 | INTACT 2, BROKEN_BY_BRANCH 2, INCONCLUSIVE 1 |
| `0020-subgen-kwargs-json-parse.patch` | 1 | INCONCLUSIVE 1 |
| `0021-entrypoint-device-guard.patch` | 1 | SKIPPED_OTHER_FILE 1 |
| `0022-runtime-config-endpoint.patch` | 4 | INTACT 1, INCONCLUSIVE 3 |
| `0023-expose-concurrent-transcriptions.patch` | 1 | INCONCLUSIVE 1 |
| `0024-per-request-ignore-forced.patch` | 14 | INTACT 4, INCONCLUSIVE 10 |
| `0025-direct-path-containment-guard.patch` | 4 | INTACT 1, INCONCLUSIVE 3 |
| `0026-detect-language-track-param.patch` | 5 | INTACT 1, INCONCLUSIVE 4 |
| `0027-config-live-model-and-swap-lock.patch` | 5 | INTACT 1, INCONCLUSIVE 4 |
| `0028-bump-patch-rev-v4.16.patch` | 1 | INCONCLUSIVE 1 |
| `0029-async-config-switch.patch` | 6 | INCONCLUSIVE 6 |
| `0030-bump-patch-rev-v4.17.patch` | 1 | INCONCLUSIVE 1 |
| `0031-fix-robust-detect-probability.patch` | 1 | INCONCLUSIVE 1 |
| `0032-bump-patch-rev-v4.18.patch` | 1 | INCONCLUSIVE 1 |
| `0034-bump-patch-rev-v4.19.patch` | 1 | INCONCLUSIVE 1 |
| `0035-bump-patch-rev-v4.20.patch` | 1 | INCONCLUSIVE 1 |
| `0036-bump-patch-rev-v4.21.patch` | 1 | INCONCLUSIVE 1 |

